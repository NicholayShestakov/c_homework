#pragma once

// Стандартная сортировка слиянием с возвратом указателя на новый, отсортированный массив.
// Принимает указатель на массив, который нужно отсортировать, и размер массива.
int* mergeSort(int* array, int size);
